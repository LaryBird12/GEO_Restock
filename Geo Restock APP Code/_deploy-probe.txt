GEO Restock — deploy-timing probe, 2026-07-30.

Temporary. Created by Fable to measure how long a push takes to become visible on
GitHub raw versus on Netlify, so the service worker redesign can be built on a
measured number instead of an assumption.

Nothing references this file. It is not part of the app. DELETE IT after the
measurement — it should ride out on the next push.

probe-id: geo-sw-timing-2026-07-30
